version: '3.8'

services:
  deep-search-api:
    build: 
      context: .
      dockerfile: Dockerfile
    ports: 
      - "8000:8000"
    environment:
      GOOGLE_API_KEY: ${GOOGLE_API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      ZAI_API_KEY: ${ZAI_API_KEY}
      SYNTHESIS_PROVIDER: ${SYNTHESIS_PROVIDER:-gemini}
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-1.5-flash}
      OPENAI_MODEL: ${OPENAI_MODEL:-gpt-4o-mini}
      ZAI_MODEL: ${ZAI_MODEL:-glm-4.7-flash}
      CACHE_ENABLED: "true"
      CACHE_DIR: /app/cache
      CACHE_TTL: 86400
      CACHE_MAX_SIZE_MB: 2000
      PLAYWRIGHT_TIMEOUT: 30
      MAX_BROWSERS: 4
      SEARCH_TIMEOUT: 30
      EXTRACTION_WORKERS: 4
      EXTRACTION_MAX_IN_FLIGHT: 8
      RERANKER_USE_CLOUD: "true"
      CHUNKING_STRATEGY: hybrid
      CHUNK_SIZE: 350
      RATE_LIMIT: 30/minute
      DEBUG: "false"
    volumes:
      - cache_data:/app/cache
      - ./logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test:  ["CMD", "curl", "-f", "http://localhost:8000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    # Resource limits for 1vCPU, 16GB RAM
    deploy: 
      resources:
        limits: 
          cpus: '1'
          memory: 14G
        reservations:
          cpus: '0.8'
          memory: 12G

volumes:
  cache_data: 
    driver: local